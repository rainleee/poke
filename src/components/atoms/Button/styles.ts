import { styled } from "@/styles/stitches.config";

export const ButtonStyled = styled("button", {
  backgroundColor: "$Red-DA343C",
  padding: "9px 16px",
  borderRadius: "6px",
  color: "#FFFFFF",
  border: "none",
  outline: "none",
  cursor: "pointer",
  fontSize: "14px",
});
